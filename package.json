{
  "name": "estudo-cypress-cucumber",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "cypress run --browser ${npm_config_browser} firefox --spec \"**/*.feature\" --no-exit",
    "cy:open": "cypress open",
    "test:all": "cypress run --browser ${npm_config_browser} --spec \"**/*.features\" --no-exit"
  },
  "devDependencies": {
    "cypress": "4.6.0",
    "cypress-cucumber-preprocessor": "4.0.0"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": true
  }
}
